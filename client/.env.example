# VibeFlo Client Environment Variables
#
# HOW TO USE THIS FILE:
# 1. Copy this file to a new file named ".env" in the client directory
# 2. Replace placeholder values with actual configuration values
# 3. Keep your .env file secure - never commit it to version control
#
# YOUTUBE API SETUP:
# 1. Create a project in Google Cloud Console
# 2. Enable YouTube Data API v3
# 3. Create an API key with appropriate restrictions:
#    - Set HTTP referrers to include: 
#      * localhost:3000/* (for local development)
#      * *.vibeflo.app/* (for production)
#    - Restrict the key to YouTube Data API v3
# 4. Copy the generated API key to REACT_APP_YOUTUBE_API_KEY below
#
# Note: Prefix all React environment variables with REACT_APP_

# API Configuration
REACT_APP_API_URL=http://localhost:5001
REACT_APP_API_TIMEOUT=10000

# YouTube API
REACT_APP_YOUTUBE_API_KEY=your_youtube_api_key

# OAuth configuration
REACT_APP_OAUTH_REDIRECT_URL=http://localhost:3000/oauth-callback

# Optional: Analytics
REACT_APP_GA_TRACKING_ID= 